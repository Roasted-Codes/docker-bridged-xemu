# Research Scripts

Debugging and memory discovery tools used during development. Not part of the production tool.

These scripts must be run from the project root or they use a `sys.path` shim to find `xbdm_client.py` in the parent directory.
